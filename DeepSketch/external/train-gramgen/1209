1 ?{not(and(or(<cap>,<low>),not(<any>)))}
2 ?{and(not(or(<cap>,<low>)),not(<any>))}
3 ?{not(<cap>),and(<low>,not(<any>))}
4 ?{not(or(<cap>,<low>)),not(<any>)}
5 ?{not(and(or(<cap>,<low>),not(<let>)))}
6 ?{not(and(or(<cap>,<low>),<any>))}
7 ?{not(and(or(<let>,<low>),not(<any>)))}
8 ?{and(not(or(<cap>,<low>)),not(<let>))}
9 ?{and(not(or(<let>,<low>)),not(<any>))}
10 ?{and(not(or(<cap>,<low>)),<any>)}
11 ?{and(or(<cap>,<low>),not(<any>))}
12 ?{not(<cap>),<low>,not(<any>)}
13 ?{not(<cap>),and(<low>,not(<let>))}
14 ?{not(<cap>),and(<low>,<any>)}
15 ?{not(<let>),and(<low>,not(<any>))}
16 ?{not(or(<cap>,<low>)),not(<let>)}
17 ?{not(or(<let>,<low>)),not(<any>)}
18 ?{not(or(<cap>,<low>)),<any>}
19 ?{not(and(or(<cap>,<low>),<let>))}
20 ?{not(and(or(<let>,<low>),not(<let>)))}
21 ?{not(and(or(<let>,<low>),<any>))}
22 ?{and(not(or(<let>,<low>)),not(<let>))}
23 ?{and(not(or(<let>,<low>)),<any>)}
24 ?{and(not(or(<cap>,<low>)),<let>)}
25 ?{<cap>,and(<low>,not(<any>))}
26 ?{or(<cap>,<low>),not(<any>)}
27 ?{and(or(<cap>,<low>),not(<let>))}
28 ?{and(or(<cap>,<low>),<any>)}
29 ?{and(or(<let>,<low>),not(<any>))}
30 ?{not(<cap>),<low>,not(<let>)}
31 ?{not(<cap>),<low>,<any>}
32 ?{not(<cap>),and(<low>,<let>)}
33 ?{not(<let>),<low>,not(<any>)}
34 ?{not(<let>),and(<low>,not(<let>))}
35 ?{not(<let>),and(<low>,<any>)}
36 ?{not(or(<let>,<low>)),not(<let>)}
37 ?{not(or(<let>,<low>)),<any>}
38 ?{not(or(<cap>,<low>)),<let>}
39 ?{not(and(or(<let>,<low>),<let>))}
40 ?{not(and(<low>,not(<any>)))}
41 ?{and(not(<low>),not(<any>))}
42 ?{and(not(or(<let>,<low>)),<let>)}
43 ?{<cap>,<low>,not(<any>)}
44 ?{<cap>,and(<low>,not(<let>))}
45 ?{<cap>,and(<low>,<any>)}
46 ?{or(<cap>,<low>),not(<let>)}
47 ?{or(<cap>,<low>),<any>}
48 ?{and(or(<cap>,<low>),<let>)}
49 ?{<let>,and(<low>,not(<any>))}
50 ?{or(<let>,<low>),not(<any>)}
