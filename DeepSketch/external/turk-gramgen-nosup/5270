1 ?{not(and(or(<let>,<low>),not(<vow>)))}
2 ?{and(not(or(<let>,<low>)),not(<vow>))}
3 ?{not(<let>),and(<low>,not(<vow>))}
4 ?{not(or(<let>,<low>)),not(<vow>)}
5 ?{not(and(or(<let>,<low>),<vow>))}
6 ?{and(not(or(<let>,<low>)),<vow>)}
7 ?{and(or(<let>,<low>),not(<vow>))}
8 ?{not(<let>),<low>,not(<vow>)}
9 ?{not(<let>),and(<low>,<vow>)}
10 ?{not(or(<let>,<low>)),<vow>}
11 ?{<let>,and(<low>,not(<vow>))}
12 ?{or(<let>,<low>),not(<vow>)}
13 ?{and(or(<let>,<low>),<vow>)}
14 ?{not(<let>),<low>,<vow>}
15 ?{not(and(<low>,not(<vow>)))}
16 ?{and(not(<low>),not(<vow>))}
17 ?{<let>,<low>,not(<vow>)}
18 ?{<let>,and(<low>,<vow>)}
19 ?{or(<let>,<low>),<vow>}
20 ?{not(<let>),and(<let>,not(<vow>))}
21 ?{not(<low>),not(<vow>)}
22 ?{not(or(<let>,<low>))}
23 ?{not(and(<low>,<vow>))}
24 ?{and(not(<low>),<vow>)}
25 ?{<let>,<low>,<vow>}
26 ?{and(<low>,not(<vow>))}
27 ?{not(<let>),<low>}
28 ?{not(<let>),<let>,not(<vow>)}
29 ?{not(<let>),and(<let>,<vow>)}
30 ?{not(<low>),<vow>}
31 ?{not(and(<let>,not(<vow>)))}
32 ?{and(not(<let>),not(<vow>))}
33 ?{<let>,and(<let>,not(<vow>))}
34 ?{or(<let>,<low>)}
35 ?{<low>,not(<vow>)}
36 ?{and(<low>,<vow>)}
37 ?{not(<let>),<let>,<vow>}
38 ?{not(<let>),not(<vow>)}
39 ?{not(and(<let>,<vow>))}
40 ?{and(not(<let>),<vow>)}
41 ?{<let>,<low>}
42 ?{<let>,and(<let>,<vow>)}
43 ?{and(<let>,not(<vow>))}
44 ?{<low>,<vow>}
45 ?{not(<let>),<vow>}
46 ?{not(<low>)}
47 ?{<let>,not(<vow>)}
48 ?{and(<let>,<vow>)}
49 ?{not(<let>),<let>}
50 ?{not(not(<vow>))}
