1 ?{contain(and(<let>,not(<low>)))}
2 ?{and(contain(<let>),not(<low>))}
3 ?{contain(<let>),not(<low>)}
4 ?{contain(and(<let>,<low>))}
5 ?{and(contain(<let>),<low>)}
6 ?{and(<let>,not(<low>))}
7 ?{contain(<let>),<low>}
8 ?{contain(not(<low>))}
9 ?{<let>,not(<low>)}
10 ?{and(<let>,<low>)}
11 ?{contain(and(<let>,not(<let>)))}
12 ?{and(contain(<let>),not(<let>))}
13 ?{contain(<low>)}
14 ?{<let>,<low>}
15 ?{not(<low>)}
16 ?{contain(<let>),not(<let>)}
17 ?{contain(and(<let>,<let>))}
18 ?{and(contain(<let>),<let>)}
19 ?{and(<let>,not(<let>))}
20 ?{<low>}
21 ?{contain(<let>),<let>}
22 ?{contain(not(<let>))}
23 ?{<let>,not(<let>)}
24 ?{and(<let>,<let>)}
25 ?{contain(<let>)}
26 ?{not(<let>)}
27 ?{<let>}
